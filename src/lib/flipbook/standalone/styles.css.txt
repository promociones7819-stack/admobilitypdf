:root {
  color-scheme: light dark;
  --bg: #0f1115;
  --panel: #171a21;
  --border: #2a2f3a;
  --fg: #e8eaee;
  --muted: #9aa3b2;
}
* { box-sizing: border-box; }
html, body { margin: 0; height: 100%; }
body {
  display: flex;
  flex-direction: column;
  background: var(--bg);
  color: var(--fg);
  font: 14px/1.45 system-ui, -apple-system, "Segoe UI", Roboto, sans-serif;
}
.bar {
  display: flex;
  align-items: center;
  gap: 6px;
  flex-wrap: wrap;
  padding: 8px 10px;
  background: var(--panel);
  border-bottom: 1px solid var(--border);
}
.title { font-weight: 600; max-width: 40vw; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
.spacer { flex: 1; }
.muted { color: var(--muted); font-size: 12px; }
.btn {
  background: transparent;
  color: var(--fg);
  border: 1px solid var(--border);
  border-radius: 8px;
  padding: 5px 10px;
  cursor: pointer;
  font-size: 13px;
}
.btn:hover { background: rgba(255, 255, 255, 0.07); }
.page-input {
  width: 56px;
  text-align: center;
  background: transparent;
  color: var(--fg);
  border: 1px solid var(--border);
  border-radius: 8px;
  padding: 5px 6px;
}
.body { display: flex; flex: 1; min-height: 0; }
.panel {
  width: 280px;
  flex: none;
  overflow: auto;
  padding: 12px;
  background: var(--panel);
  border-right: 1px solid var(--border);
}
.panel h2 { margin: 0 0 8px; font-size: 13px; text-transform: uppercase; color: var(--muted); }
.panel ul { list-style: none; margin: 0; padding: 0; }
.panel button {
  width: 100%;
  text-align: left;
  background: transparent;
  border: 0;
  color: var(--fg);
  padding: 6px 8px;
  border-radius: 6px;
  cursor: pointer;
  font-size: 13px;
}
.panel button:hover { background: rgba(255, 255, 255, 0.07); }
.panel .pg { color: var(--muted); font-size: 11px; margin-left: 6px; }
.stage {
  flex: 1;
  min-width: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  overflow: auto;
  padding: 12px;
  position: relative;
}
.status { position: absolute; color: var(--muted); }
.book { box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.6); }
.page-container { position: relative; overflow: hidden; background: #fff; }
.hotspot { position: absolute; display: block; cursor: pointer; }
.hotspot-3d {
  display: flex; align-items: center; justify-content: center;
  color: #fff; font-size: clamp(16px, 3vw, 32px); font-weight: 900; text-decoration: none;
  border: 1px solid rgba(255,255,255,.55); border-radius: 14px;
  background: linear-gradient(145deg,#60a5fa 0%,#2563eb 46%,#1e40af 100%);
  box-shadow: inset 0 2px 1px rgba(255,255,255,.55), inset 0 -4px 5px rgba(15,23,42,.3), 0 5px 0 #172554, 0 8px 14px rgba(15,23,42,.38);
  text-shadow: 0 2px 2px rgba(15,23,42,.5);
  transition: filter .15s, transform .15s, box-shadow .15s;
}
.hotspot-3d:hover { filter: brightness(1.12); transform: translateY(-1px); }
.hotspot-3d:active { transform: translateY(4px); box-shadow: inset 0 2px 4px rgba(15,23,42,.35),0 1px 0 #172554; }
.hotspot-3d-circle { border-radius: 999px; }
.hotspot-3d-arrow { border-radius: 14px; }
.hotspot-3d-brand {
  overflow: hidden; padding: 4px; border-color: rgba(22,101,52,.45);
  background: linear-gradient(145deg,#fffef9 0%,#f4f1e7 55%,#dfe9df 100%);
  box-shadow: inset 0 2px 2px rgba(255,255,255,.9), inset 0 -4px 5px rgba(22,101,52,.18), 0 5px 0 #166534, 0 8px 15px rgba(6,68,95,.32);
}
.hotspot-3d-brand img { width: 100%; height: 100%; object-fit: contain; pointer-events: none; }
.page-edge {
  position: absolute; z-index: 20; top: 50%; width: 28px; height: 88px;
  transform: translateY(-50%); border: 0; opacity: .55; cursor: grab;
  transition: opacity .2s;
}
.page-edge:hover { opacity: 1; }
.page-edge.left { left: 0; background: linear-gradient(90deg, rgba(15,23,42,.24), transparent); }
.page-edge.right { right: 0; background: linear-gradient(270deg, rgba(15,23,42,.24), transparent); }
.overlay {
  position: fixed;
  inset: 0;
  background: rgba(0, 0, 0, 0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 24px;
  z-index: 50;
}
.overlay[hidden] { display: none; }
.card {
  max-width: 560px;
  background: var(--panel);
  border: 1px solid var(--border);
  border-radius: 14px;
  padding: 20px 22px;
}
.card h2 { margin-top: 0; font-size: 18px; }
.card code {
  background: rgba(255, 255, 255, 0.08);
  padding: 1px 5px;
  border-radius: 5px;
}
